// Legacy compatibility stub: auth implementation moved to `src/app/auth/config.ts` and
// App Router routes under `src/app/api/auth/[...nextauth]/route.ts`.
// Keep this file as a harmless stub to avoid tooling looking for the old shape.

export const __legacyAuthStub = true;
